# Test Environment Configuration

# Application
NODE_ENV=test
PORT=3001

# Test Database (puerto 5433)
DATABASE_URL="postgresql://taskify_user:taskify_password@localhost:5433/taskify_test_db"

# JWT Configuration (same as development for testing)
JWT_SECRET="your-super-secret-jwt-key-with-at-least-32-characters"
JWT_REFRESH_SECRET="your-super-secret-refresh-jwt-key-with-at-least-32-characters"
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES="7d"

# Redis (optional for tests)
REDIS_URL="redis://localhost:6379"